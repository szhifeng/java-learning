plugins {
    id 'buildlogic.spring-build-conventions'
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

application {
    // Define the main class for the application.
    mainClass = 'com.example.concurrent.Application'
}